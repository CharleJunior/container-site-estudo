services:
  web:
    build: .
    container_name: django_sistema
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    command: >
      bash -c "python manage.py migrate --fake-initial &&
               python manage.py runserver 0.0.0.0:8000"